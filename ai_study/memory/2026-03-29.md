# 2026-03-29 项目进展

## 当前系统状态
- 后端 API: localhost:8080 ✅ 运行中
- 前端: localhost:3000 ✅ 运行中

## 项目结构
```
ai_study/
├── backend/          # Go + Gin + Temporal + gRPC
│   ├── internal/     # handlers, models, db, websocket, github, temporal
│   ├── python_agent/ # gRPC stub (PM/Dev/Test Agent 核心逻辑缺失)
│   └── cmd/api/      # HTTP server
├── frontend/         # Next.js
│   └── src/app/      # page.tsx, kanban, delivery, login (UI 框架完成)
│   └── src/lib/      # api.ts (mock数据), kanban-store.ts, store.ts
│   └── src/components/
└── docs/prd/         # PRD 文档齐全
```

## 已完成
- 前端骨架（4个页面 UI 框架）
- WebSocket 实时通信（后端）
- Temporal 工作流框架
- GitHub OAuth 框架
- 数据库 schema
- gRPC proto

## 核心缺失
1. **Python Agent 核心逻辑** — python_agent/ 只有 gRPC stub，三个 Agent(PM/Dev/Test)业务逻辑不存在
2. **前端 API 路由** — app/api/ 下只有 auth/，任务看板/交付无 API 路由
3. **前后端对接** — 需求提交→Agent→前端看板，数据链路未通
4. **评审流程自动化** — 4级评审状态机触发未实现

## 下一步建议
- 方案A：打通前后端数据流，Agent 先 stub
- 方案B：补全 Python Agent 核心逻辑

